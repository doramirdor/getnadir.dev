# Core FastAPI and web framework
fastapi==0.103.1
uvicorn==0.23.2
pydantic>=2.5.0,<3.0.0
python-dotenv==1.0.0
python-multipart>=0.0.6  # For form data
jinja2>=3.1.0  # For templates

# Authentication and Database
supabase>=2.0.0  # Supabase client for auth and data

# LLM Providers and Routing
litellm>=1.50.0  # Unified LLM provider access
openai>=1.0.0
anthropic>=0.5.0
google-generativeai>=0.3.0
tiktoken>=0.4.0  # For token counting

# ML and Complexity Analysis
sentence-transformers>=2.2.2  # For embedding-based caching
scikit-learn>=1.2.2  # For ML analyzers
torch>=1.12.0  # For ML analyzers (CPU only)
transformers>=4.20.0  # For BERT analyzer
numpy>=1.21.0

# Background Tasks
celery>=5.3.0
fakeredis>=2.10.0  # Redis mock for local development

# Monitoring
prometheus-client>=0.20.0

# Utilities
requests>=2.28.2
httpx>=0.24.0
tenacity>=8.2.2  # For retries

# Testing (development only)
pytest>=7.3.1
pytest-asyncio>=0.21.0 
[build]
  base = "app"
  command = "npm ci && npm run build"
  publish = "dist"

# SPA fallback — all routes handled by React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
